name: Run Tests

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Run tests
        run: pytest tests/ -v

  # After tests pass, trigger ingester rebuilds (main and dev)
  trigger-ingester-builds:
    needs: test
    if: github.event_name == 'push'
    runs-on: ubuntu-latest

    strategy:
      matrix:
        repo:
          - wesense-ingester-meshtastic
          - wesense-ingester-wesense
          - wesense-ingester-homeassistant
          - wesense-zenoh-bridge

    steps:
      - name: Trigger rebuild of ${{ matrix.repo }}
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.CORE_DISPATCH_TOKEN }}
          repository: wesense-earth/${{ matrix.repo }}
          event-type: core-updated
          client-payload: '{"core_branch": "${{ github.ref_name }}"}'
